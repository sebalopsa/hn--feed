version: "2"
services:
  server:
    build: server/
    ports:
      - 3000:3000
  client:
    build: client/
    ports:
      - 8080:80
  db:
    image: mongo:latest
    environment:
      - MONGODB_DATABASE="nh-feed"
    ports:
      - 27017:27017
